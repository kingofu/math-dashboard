# 导航项目添加与管理工作流说明

为了方便后续维护和更新 `AI数学教学与学术工具导航.html` 仪表盘中的项目，本项目配备了自动化脚本 `add_project.py`。该脚本负责从网址（尤其是 GitHub 仓库）抓取元数据、自动翻译/覆盖文案、自动分类与打标签，并将最终数据写入 `data.js`。

---

## 1. 基础运行环境

脚本基于 Python 3 运行，需安装以下第三方库：
```bash
pip install requests beautifulsoup4
```

---

## 2. 常用操作指令

脚本位于 `web_dashboard` 目录下，所有操作均在该目录下执行：

### 2.1 预览模式（Dry Run）
在正式写入 `data.js` 之前，建议先运行预览，以检查抓取的信息、分类结果以及是否包含英文简介翻译请求：
```bash
python add_project.py <项目URL> --dry-run
```
* **输出示例**：会返回一段包含 `project` 详情、`complexity_hint`（如 `[🌍 翻译请求]`）的 JSON 数据，且不会修改 `data.js`。

### 2.2 正式添加项目
若预览无误，直接运行以下命令即可将项目追加至 `data.js` 末尾：
```bash
python add_project.py <项目URL>
```
* 脚本会自动计算新的 `num` 索引并完成写入。

### 2.3 自定义属性添加
如需手动指定名称、中文描述或星标数（尤其是遇到非 GitHub 链接无法自动获取星标或需要人工翻译英文描述时），可使用以下可选参数：

* `--name`: 自定义项目名称
* `--desc`: 自定义项目中文描述
* `--stars`: 自定义星标数（如 `12k` 或 `88`）

**命令示例**：
```bash
python add_project.py https://github.com/Ge-Shun/origin-mcp --desc "本地 MCP 服务器，让 AI 助手能够自动化操作 Origin/OriginPro 进行数据和图像处理。"
```

### 2.4 删除项目
如需从列表中移除某个项目，可以通过项目编号（`num`）、精确 URL 或精确项目名称进行删除：
```bash
python add_project.py --delete <项目编号 / URL / 项目名称>
```
* **注意**：删除后，脚本会自动为剩余的所有项目重新排序 `num` 索引，确保编号连续。

---

## 3. 自动化规则与逻辑说明

### 3.1 自动分类规则 (`KEYWORD_RULES`)
脚本通过扫描项目的名称、描述和 GitHub Topics 中包含的关键字进行打分，并归入得分最高的一类：
* **`tutoring` (智能教学与启发式辅导)**: `socratic`, `tutor`, `classroom` 等教学辅助词。
* **`visualization` (数学可视化与动画生成)**: `manim`, `anim`, `visual`, `可视化` 等。
* **`formatting` (教案与文档自动化)**: `latex`, `docx`, `pdf`, `ppt`, `markdown`, `排版` 等。
* **`academic_writing` (学术写作与文献检索)**: `research`, `paper`, `arxiv`, `学术`, `论文` 等。
* **`math_modeling` (形式化数学与数模竞赛)**: `mathlib`, `lean 4`, `modeling`, `建模`, `竞赛` 等。
* **`agent` (AI智能体与集成框架)**: `agent`, `mcp`, `plugin`, `developer`, `api` 等。
* **`other` (其他学术与教学工具)**: 兜底分类。

### 3.2 标签自动生成 (`TAG_RULES`)
根据匹配规则，项目将被标记为以下一个或多个常用标签：
* `数学动画` / `可视化` / `苏格拉底教学` / `智能助教` / `文档自动化` / `数学建模` / `学术科研` / `开发工具` / `AI智能体`
* 若未匹配到任何标签，则默认标记为 `通用工具`。

### 3.3 重复性校验与安全保护
* **去重校验**：在添加前，脚本会进行 URL 标准化（忽略大小写、尾部斜杠及参数），若已存在，则阻止重复写入并发出警告。
* **英文简介检测**：若检测到抓取的简介为英文，脚本会输出提示 `[🌍 翻译请求]`，需人工翻译为中文后使用 `--desc` 参数再次执行。
* **堆积预警 (`crowding_alerts`)**：若某个分类或标签下的项目数量过多（例如，单标签下的项目达到或超过 8 个，或者“其他”分类项目超过 15 个），控制台将输出堆积预警，提示可能需要细化子分类。
